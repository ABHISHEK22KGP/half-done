<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .loansdetails-div {
            background-color: #f8f9fa; /* Light background */
            border: 2px solid #007bff; /* Blue border */
            padding: 15px;
            margin: 10px 0;
            border-radius: 10px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            font-family: Arial, sans-serif;
            transition: transform 0.2s ease-in-out;
        }

        .loansdetails-div:hover {
            transform: scale(1.02);
            box-shadow: 4px 4px 15px rgba(0, 0, 0, 0.2);
        }

        .loansdetails-div p {
            margin: 5px 0;
            font-size: 16px;
            color: #333;
        }

        .loansdetails-div strong {
            color: #007bff;
        }
    </style>
</head>
<body>
    <% rate.rates.forEach(rate => { %>
        <div class="loansdetails-div">
            <div class="rate-details">
                <p><strong>Principal:</strong> ₹<%= rate.principal %></p>
                <p><strong>Date Taken:</strong> <%= new Date(rate.dateTaken).toLocaleDateString() %></p>
                <p><strong>Interest:</strong> ₹<%= rate.interest %></p>
                <p><strong>Date Difference:</strong> <%= rate.date_diff %></p>
            </div>
        </div>
    <% }) %>
    <script>
        //js
    </script>
</body>
</html>


